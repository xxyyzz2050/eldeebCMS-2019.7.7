# See http://help.github.com/ignore-files/ for more about ignoring files.

node_modules
__old
*.lnk
temp
tmp
*.cache
*.log
/.firebase
**/*.js.map
config.ts
Thumbs.db
.sass-cache

#outputs
dist
out-tsc
bazel-out

# IDEs
.idea
.project
.classpath
.settings/
*.launch
.history/*
.vscode/*

# notes:
# - don't ignore package-lock.json because it used by npm to install the exactly same versions of dependencies, https://stackoverflow.com/a/50868690
# - add any file contains sensitive data, such as config.ts
#---------------------------
